const LandsatNaturalColorLayer=L.GridLayer.extend({createTile(coords,done){const tile=document.createElement('img');tile.alt='';const size=256,half=20037508.342789244,n=2**coords.z,span=(half*2)/n,minX=-half+coords.x*span,maxX=minX+span,maxY=half-coords.y*span,minY=maxY-span;const params=new URLSearchParams({f:'image',bbox:`${minX},${minY},${maxX},${maxY}`,bboxSR:'3857',imageSR:'3857',size:`${size},${size}`,format:'jpgpng',interpolation:'RSP_BilinearInterpolation',renderingRule:JSON.stringify({rasterFunction:'Natural Color'})});tile.onload=()=>done(null,tile);tile.onerror=()=>done(new Error('Falha ao carregar Landsat'),tile);tile.src=`https://landsat2.arcgis.com/arcgis/rest/services/Landsat8_Views/ImageServer/exportImage?${params.toString()}`;return tile;}});basemaps.landsat=new LandsatNaturalColorLayer({tileSize:256,minZoom:2,maxZoom:16,attribution:'Landsat imagery — Esri, USGS, NASA · Natural Color RGB 4-3-2'});